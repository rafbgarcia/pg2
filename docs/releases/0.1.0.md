# Release Tracker: 0.1.0

Last updated: 2026-02-20
Owner: Codex session (handoff-ready)
Status: In progress

## Release Theme

String overflow safety + deterministic reclaim foundation through server-session E2E coverage.

## Scope

1. Auto page compaction on update/insert shortfall.
2. Hybrid string storage:
   - Inline small strings.
   - Spill large strings to overflow pages.
3. Deterministic and crash-safe reclaim for replaced/deleted overflow chains.

## Confirmed Product Decisions

- Encoding: UTF-8.
- Inline threshold target for `string`: 1024 bytes.
- Overflow model: single overflow chain per spilled field value.
- Reclaim policy: immediate logical unlink + deterministic reclaim pipeline (not synchronous full physical reclaim inside mutation path).

## Clarifications

- `string` has no declared max as a logical type contract.
- Physical storage policy is independent and uses byte thresholds, not character counts.
- 1024 is bytes, so max character count varies by UTF-8 code points.

## Done Definition

Release `0.1.0` is done only when all conditions below are true:

1. Scope completion:
   - Auto-compaction + hybrid inline/overflow + deterministic reclaim behavior are complete through server session E2E coverage.
2. Remaining core gaps closed:
   - Transaction abort/rollback semantics are explicitly defined and tested for overflow create/relink/unlink/reclaim lifecycle.
3. Quality-gate discipline:
   - Every core DB increment in this release has a corresponding artifact under `docs/releases/0.1.0/gates/`.
   - No placeholder commit markers remain in release or gate docs.
4. User-facing docs parity:
   - Any user-visible behavior or error/inspect output changes are documented in `user-facing-docs/` in the same increment.
5. Validation:
   - `zig build test` passes at release close with overflow E2E coverage included.

## Known Test State

- `zig build test` passes for the latest increment (includes strict tx-marker replay fail-closed coverage, commit-hook reclaim policy tests, and crash/fault matrix assertions for overflow reclaim replay outcomes).
- Server E2E string-overflow spec file was renamed for clarity: `src/server/e2e/overflow.zig` -> `src/server/e2e/string_overflow.zig`.

## Progress Snapshot

Completed:

1. Overflow reclaim pipeline.
2. WAL lifecycle contract for create/relink/unlink/reclaim.
3. Deterministic crash/restart coverage for spill/replace/delete.
4. Malformed/cyclic chain fail-closed reclaim coverage.
5. Inspect-level reclaim backlog/throughput visibility.
6. Recovery replay application for overflow lifecycle WAL with idempotent reclaim semantics.
7. Server E2E overflow test module rename for explicit string-overflow scope.
8. Recovery replay abort matrix tests for overflow create/relink/unlink/reclaim lifecycle records (aborted tx records are non-replayable).
9. Mutation-path rollback contract for overflow reclaim queue entries, including deterministic rollback ordering tests and abort-safe live-chain protection.
10. Queue-drain budget semantics locked for multi-chain unlink: fixed one committed reclaim per successful commit boundary with deterministic inspect backlog progression.
11. Strict tx-marker replay policy implemented: overflow lifecycle replay now fails closed on markerless mutation WAL, and server pool emits tx begin/commit/abort WAL markers.
12. Reclaim-drain execution policy finalized as commit-hook only with deterministic budget (one committed chain per successful write commit boundary).
13. Added server-path crash/fault matrix E2E coverage for overflow reclaim replay outcomes across crash checkpoints.
14. Reclaim durability finalized: queue drain now advances only on successful write commits, with crash/restart and repeated replay matrix coverage proving durable multi-chain reclaim + idempotence.

Open:

- none (release-scoped overflow durability gaps resolved in current implementation and E2E matrix coverage).

## Increment Log

Committed SHA(s) in latest increment:

- `eda1361` - Lock overflow reclaim backlog budget semantics.
- `f7f33a2` - Implement tx-aware overflow reclaim rollback contract.
- `48fe07b` - Enforce strict tx-marker replay and commit-hook-only overflow reclaim drain.
- `95a1e4d` - Add overflow reclaim crash/fault matrix E2E coverage and document replay durability gap.
- `1f0cd08` - Finalize durable write-commit reclaim drain semantics and repeated replay matrix coverage.

## Next-Session Kickoff (Concrete)

1. Decide whether to close `0.1.0` now or run an extra seed-sweep variant of crash matrix before close.
2. If closing now, set final release status and keep only release-close handoff commands.
3. Run placeholder hygiene check before finalizing docs.

## Fresh Codex Handoff Commands

1. `git status --short`
2. `zig build test`
3. `git log -6 --stat`
12. `git show --name-only --stat eda1361`
13. `rg -n "overflow_chain_create|overflow_chain_relink|overflow_chain_unlink|overflow_chain_reclaim|replay|recover|tx_abort|commitOverflowReclaimEntriesForTx|rollbackOverflowReclaimEntriesForTx" src`
14. `rg -n "INSPECT overflow|overflow_reclaim_stats|snapshotOverflowReclaimStats|overflow_reclaim_queue|abortCheckin" src docs user-facing-docs`
15. `rg -n 'Commit: .*TBD|pending commit|TBD \\(this increment commit\\)|Commit: <pending>|<sha>|<answer>|<none or details>' docs/releases`
16. Continue from the open acceptance criteria above.
