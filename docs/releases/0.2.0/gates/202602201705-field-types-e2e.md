# Quality Gate Artifact: 202602201705-field-types-e2e

- Artifact ID: `202602201705-field-types-e2e`
- Commit: `21534be`
- Release: `0.2.0`
- Title: `Server session-path E2E coverage for available field types`
- Scope: `Add one dedicated server E2E test file per currently supported schema field type.`

## PR Checklist

- What invariant was added or changed?
  - Server session-path E2E coverage now includes one scenario per supported schema field type under `src/server/e2e/field_types/`:
    - `bigint.zig`
    - `boolean.zig`
    - `string.zig`
  - Suite aggregation updated in `src/server/e2e/e2e_specs.zig` so these tests are always discovered.

- What is the crash-consistency contract for the modified path?
  - No crash contract behavior change in this increment; this is coverage-only work over existing write/read paths.

- Which error classes can now be returned?
  - No new user-visible error classes introduced.

- Does this change modify any persistent format or protocol?
  - Persistent format: `none`
  - Protocol: `none`

- Which deterministic crash/fault tests were added?
  - None in this increment.
  - Deterministic server-session E2E tests added:
    - `e2e bigint fields preserve large integer values end-to-end`
    - `e2e boolean fields support operational toggle workflows`
    - `e2e string fields preserve user-facing text values`

- Which performance baseline or threshold was updated (if any)?
  - `none` (no performance contract change; coverage-focused increment).
